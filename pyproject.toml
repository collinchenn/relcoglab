[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "relcoglab"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "transformer-lens",
]

[tool.setuptools.packages.find]
where = ["src"]